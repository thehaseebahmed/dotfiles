services:
  wakapi:
    image: ghcr.io/muety/wakapi:2.18.0
    init: true
    ports:
      - 3222:3000
    restart: unless-stopped
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy
