services:
  wakapi:
    image: ghcr.io/muety/wakapi:2.17.6
    init: true
    ports:
      - 3222:3000
    restart: unless-stopped
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy
