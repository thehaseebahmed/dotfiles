services:
  profilarr:
    image: santiagosayshey/profilarr:v1.1.4
    container_name: profilarr

    ports:
      - 6868:6868

    volumes:
      - ~/volumes/profilarr/config:/config

    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/Amsterdam

    restart: unless-stopped
